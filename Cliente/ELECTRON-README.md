# 🎓 نظام إدارة المدرسة - تطبيق Desktop

## 📦 التثبيت والتشغيل

### 1. تثبيت Dependencies:
```bash
npm install
```

### 2. تشغيل التطبيق في وضع التطوير:
```bash
npm run electron-dev
```

### 3. بناء التطبيق للإنتاج:
```bash
npm run build-electron
```

### 4. إنشاء ملف التثبيت:
```bash
npm run dist
```

## 🚀 المميزات الجديدة في النسخة Desktop:

### ✅ **واجهة سطح المكتب:**
- **نافذة مخصصة** - حجم مثالي للتطبيق
- **قائمة عربية** - قوائم بالعربية مع اختصارات لوحة المفاتيح
- **أيقونة مخصصة** - أيقونة التطبيق في شريط المهام
- **حفظ محلي** - البيانات محفوظة على الجهاز

### ⚡ **الأداء:**
- **تحميل سريع** - لا حاجة لمتصفح
- **استهلاك ذاكرة أقل** - مقارنة بالمتصفح
- **عمل بدون إنترنت** - بعد التحميل الأولي

### 🔒 **الأمان:**
- **حماية من XSS** - Context isolation مفعل
- **منع تشغيل Node.js** - في renderer process
- **روابط خارجية آمنة** - تفتح في المتصفح الافتراضي

## 📋 **قوائم التطبيق:**

### 📁 **قائمة ملف:**
- **إعدادات المدرسة** - `Ctrl+,`
- **خروج** - `Ctrl+Q`

### 👁️ **قائمة عرض:**
- **إعادة تحميل** - `Ctrl+R`
- **أدوات المطور** - `F12`
- **تكبير/تصغير** - `Ctrl++` / `Ctrl+-`
- **ملء الشاشة** - `F11`

### 🪟 **قائمة نافذة:**
- **تصغير** - `Ctrl+M`
- **إغلاق** - `Ctrl+W`

### ❓ **قائمة مساعدة:**
- **حول التطبيق** - معلومات النسخة

## 🛠️ **البناء للمنصات المختلفة:**

### Windows:
```bash
npm run dist -- --win
```

### macOS:
```bash
npm run dist -- --mac
```

### Linux:
```bash
npm run dist -- --linux
```

## 📁 **بنية المشروع:**

```
Cliente/
├── electron/
│   ├── main.js          # العملية الرئيسية للـ Electron
│   └── assets/          # أيقونات وموارد التطبيق
├── src/                 # كود React
├── dist/                # البناء النهائي
├── dist-electron/       # ملفات التثبيت
└── package.json         # إعدادات المشروع
```

## 🔧 **إعدادات مخصصة:**

### تغيير أيقونة التطبيق:
1. ضع الأيقونة في `electron/assets/icon.png`
2. أعد البناء: `npm run build-electron`

### تخصيص النافذة:
عدّل في `electron/main.js`:
```javascript
new BrowserWindow({
  width: 1400,    // العرض
  height: 900,    // الارتفاع
  minWidth: 1200, // أقل عرض
  minHeight: 800  // أقل ارتفاع
});
```

## 🚨 **ملاحظات مهمة:**

1. **تأكد من تشغيل الخادم الخلفي** على `localhost:3000`
2. **البيانات محفوظة محلياً** في localStorage
3. **النسخة Desktop تعمل بدون إنترنت** بعد التحميل الأولي
4. **ملفات التثبيت** تُحفظ في مجلد `dist-electron`

## 📞 **الدعم الفني:**

إذا واجهت أي مشاكل:
1. تأكد من تثبيت Node.js (الإصدار 16+)
2. احذف `node_modules` وأعد التثبيت
3. تأكد من عمل الخادم الخلفي

---

**🎯 الآن لديك تطبيق Desktop كامل لإدارة المدرسة! 🎓**
